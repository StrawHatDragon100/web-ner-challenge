import { FormatNerPipe } from './format-ner.pipe';

describe('FormatNerPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatNerPipe();
    expect(pipe).toBeTruthy();
  });
});
